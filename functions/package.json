{
  "name": "talmaci-functions",
  "description": "Server-side proxy for the Google Cloud Translation API — no key to manage, authenticates via the function's own service account.",
  "private": true,
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "@google-cloud/translate": "^8.5.0",
    "firebase-functions": "^5.0.0"
  }
}
